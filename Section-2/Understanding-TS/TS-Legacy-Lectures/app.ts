//node js types are not built into TypeScript unlike DOM types
//To install types we need install packages "npm install @types/node or @types/react"
import fs from 'node:fs';

// fs.readFileSync();

console.log("Hello World");